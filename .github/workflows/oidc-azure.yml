name: Deploy to Azure (OIDC)

on:
  push:
    branches: [main]
  workflow_dispatch:

# TODO: Add the permissions required for OIDC
permissions:
  id-token: write  
  contents: read 

jobs:
  deploy:
    runs-on: ubuntu-latest

    # TODO: Set the environment to 'production'
    environment: production

    steps:
      - uses: actions/checkout@v4

      # TODO: Login to Azure using OIDC (no client-secret!)
      # Use azure/login@v2
      # Required parameters:
      #   client-id: from secrets (public ID, not a secret)
      #   tenant-id: from secrets
      #   subscription-id: from secrets
      - uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      #     client-id: ${{ secrets.AZURE_CLIENT_ID }}
      #     tenant-id: ${{ secrets.AZURE_TENANT_ID }}
      #     subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      # TODO: Verify the login works
      - name: Verify Azure connection
        run: |
          az account show
          az group list --output table

      # TODO: Deploy something (e.g. list resources in your resource group)
      - name: Deploy
        run: |
          az group show --name AZ-Training-17 --output table

      - name: Placeholder
        run: echo "TODO - implement OIDC login and deployment"
